// API base URL - empty when served from same origin (npm start)
// Use 'http://localhost:3000' when opening HTML files directly
const API_BASE = window.location.protocol === 'file:' ? 'http://localhost:3000' : '';
